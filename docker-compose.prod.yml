version: "3.3"

services:
  nginx:
    image: alonecat/photo-stock-nginx:latest
    restart: always

  nuxt-app:
    image: alonecat/photo-stock-frontend:latest
    restart: always
    environment:
      NODE_ENV: 'production'
